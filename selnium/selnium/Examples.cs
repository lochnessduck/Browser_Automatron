using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using OpenQA.Selenium;

namespace selnium
{
    class Examples
    {
        /*
        --//convert IWebElement to jquery object
        string jscript = "var o = $(arguments[0]);";

        --//convert IWebElement to js object
        string jscript = "var o = arguments[0];";

        --//pass IWebElement to javascript executor
        IWebElement element;
        IJavaScriptExecutor js = (IJavaScriptExecutor)driver;
        js.ExecuteScript(jscript, element);
      */
    }
}
